
server:
    port: 8080

spring:
    datasource:
        url: jdbc:postgresql://d7khvujbc2fs73dpaim0-a.oregon-postgres.render.com:5432/santos_db_9222
        username: banco_kusb_user
        # colocar no git ignore para niguem ver a senha, não expor a senha ou colocar variáveis de ambiente
        # ou repositório privado
        password: HuSj3SWgBFZS3GsxNbC2iXB6cdVeaxmZ
        driver-class-name: org.postgresql.Driver
        initialize: true

    jpa: 
        hibernate:
            ddl-auto: create-drop
            generate-ddl: true

    sql:
        init:
            schema-locations: classpath:/schema-postgresql.sql
            mode: always
            platform: postgres


